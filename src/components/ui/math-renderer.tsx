'use client'

/**
 * Legacy re-export — delegates to RichContentRenderer (the single global renderer).
 * @deprecated Use RichContentRenderer directly.
 */

import RichContentRenderer from '@/components/ui/rich-content-renderer'
export default RichContentRenderer
