name: sikkha_api_client
description: Flutter API client for শিক্ষা বাংলা (Sikkha Bangla) — Bangladesh's online education platform.
version: 1.0.0
homepage: https://sikkhabangla.com
repository: https://github.com/sikkhabangla/flutter-sdk

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  cookie_jar: ^4.0.0
  http_interceptor: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
